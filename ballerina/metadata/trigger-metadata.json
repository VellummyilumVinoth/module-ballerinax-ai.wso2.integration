{
  "version": "v1.0",
  "listeners": [
    {
      "id": "$listener",
      "doc": "A CloudVoiceListener for handling voice agent connections over WebSocket.",
      "type": {
        "name": "CloudVoiceListener"
      },
      "services": [
        "voiceService"
      ],
      "multipleServicesAllowed": true,
      "multipleServicesOfSameTypeAllowed": true
    }
  ],
  "serviceTypes": [
    {
      "id": "voiceService",
      "doc": "A voice agent service attached to a CloudVoiceListener. Each transcribed user utterance from a voice connection is delivered to the onChatMessage remote function along with the session ID, and the returned string is sent back to the caller as the spoken reply.",
      "type": {
        "name": "VoiceService"
      },
      "concrete": true,
      "multipleListenersAllowed": false,
      "handlers": {
        "backedByConcreteType": true
      }
    }
  ]
}
